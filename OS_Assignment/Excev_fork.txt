#include<stdio.h>
#include<unistd.h>
void main()
{
        char *argument[]={"/bin/ls","-l",NULL};
        int pid;
        pid=fork();

        if(pid>0)
        {
                while(1)
                {

                        printf("parent still runs \n");
                        sleep(2);
                }
        }
        else

        {
           execv("/bin/ls",argument);
        }
}